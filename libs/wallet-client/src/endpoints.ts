/**
 * command to sign a given value.
 */
export const WALLET_SIGN = 'sign';

/**
 * command to receive the public key.
 */
export const WALLET_PUB_KEY = 'pubKey';

/**
 * command to set the new id of the did.
 */
export const WALLET_SET_ID = 'set_id';

/**
 * command to return the id of the did
 */
export const WALLET_GET_ID = 'get_id';
