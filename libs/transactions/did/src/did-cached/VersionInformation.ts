/**
 * Describes the values to define a version of a did document.
 */

export class VersionInformation {
  /**
   * Numeric version of a document
   */
  id?: number;
  /**
   * unix timestamp of the version
   */
  time?: string;
}
